![[Pasted image 20241013144551.png]]

	创建单词列表的常见方法时命令替换。由命令的输出结果作为单词列表。
	
![[Pasted image 20241013150102.png]]
	
	上面的命令替换 $(strings "$1") 并没有放入双引号中，这是因为正是要将结果进行单词分割。
	一般而言，无需单词分割的话，必须将命令替换或参数放入双引号中。

	如果忽略for命令中的 in words部分，则默认处理位置参数。
	
![[Pasted image 20241013150948.png]]

##### for的C语言形式
![[Pasted image 20241013151137.png]]