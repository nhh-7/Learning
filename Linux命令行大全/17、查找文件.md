	locate：按照路径名查找文件
	find：在目录中查找文件
	xargs：通过标准输入构建并执行命令
	touch：修改文件时间
	stat：显示文件或文件系统状态
##### locate-简单的文件查找方法
	locate命令对路径执行快速的数据库查找。
	
![[Pasted image 20240925205946.png]]
	查找以zip开头的所有程序

##### find-复杂的文件查找方法
	可为find指定一个或多个目录作为查找范围
	这会输出指定目录下的所有文件
###### 测试条件
![[Pasted image 20240925210533.png]]
	加入条件使查找更精确。上例条件为类型为目录的文件、类型为普通文件的文件。
	
	各文件类型如下：
	
![[Pasted image 20240925210706.png]]

	还有许多其他类型的条件可加：
	
![[Pasted image 20240925210824.png]]
	上例为查找文件名以JPG为后缀，文件大小大于1M的普通文件。（+号代表大于，-号代表小于）
	![[Pasted image 20240925210948.png]]
	![[Pasted image 20240925211000.png]]
###### 逻辑操作符
![[Pasted image 20240925212718.png]]
	若在两个测试条件之间没有使用操作符，则默认使用-and
![[Pasted image 20240925212841.png]]
	上面的命令查找所有 普通文件且权限不为0600 或者 目录文件且权限不为0700
###### 预定义操作
	使用预定义操作可以对find查找出来的结果执行某些操作。

![[Pasted image 20240925213427.png]]
![[Pasted image 20240925213934.png]]
	上面几个命令，表示输出后缀名为.bak的普通文件，且删除。
	由于默认连接符是and，所以每个测试条件之间的顺序是很重要的，好的顺序可以提高执行效率。
	预定义操作要放在所有测试条件执行完之后执行。
###### 用户自定义操作
	用户可以使用 -exec 和 -ok调用任何命令

![[Pasted image 20240925214532.png]]
	上面使用-ok，调用了rm和ls命令，使用-ok命令行会在执行该命令之前提示用户是否继续。
	使用-exec将没有提示信息。
	 {}  代表当前路径名的符号，也就是find查找出来的路径
	  ； 作为分隔符。
	  {}和；对Shell具有特殊含义，所以必须对其进行标注或转义。
###### 提高效率
	当使用-exec操作时，都是每找到一个匹配文件，就执行一次操作。
	而我们更希望能够将所有的查找结果合并在一起，再执行操作，这样只用执行一次。
	方法：
	1.将前面命令中的分号改成加号，就能让find命令将查找结果组合成参数列表，供指定的命令一次性使用。+号无需标注或转义
	
![[Pasted image 20240925215859.png]]
	结果没有变化，但系统只执行一次ls命令。
###### xargs
	提高效率的第二种方法就是使用xargs。
	xargs能够从标准输入接受输入，将其转换为指定命令的参数列表。
	
![[Pasted image 20240925220302.png]]
	使用管道，将find命令的输出结果传给xargs，xargs构造出ls命令的参数列表。
##### touch
	touch命令通常用于设置或更新文件的访问、变更、修改时间。但如果文件名参数指定的文件并不存在，则创建同名的空文件。
##### stat
	stat可当作ls命令的增强版，它可以显示出文件的所有信息及其属性。
##### find命令选项
	用于控制find命令查找范围的选项
	
![[Pasted image 20240925224358.png]]