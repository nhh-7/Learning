	Shell提供了一组名为位置参数的变量，其中包含了命令行上的各个单词。
	
![[Pasted image 20241013113859.png]]
![[Pasted image 20241013113846.png]]

###### 确定参数个数
	参数个数存于变量 $# 中。
	
![[Pasted image 20241013114039.png]]
![[Pasted image 20241013114051.png]]
###### shift-访问多个参数
	执行一次shift命令，所有的参数就将“左移一个位置”。$2 的值移入 $1， $3 的值移入 $2,且 $# 的值也会相应减一。
	比如原来有 a b c三个参数在$1 $2 $3位置上，执行shift后，b c  将回到$1 $2 $3位置上。左移一位。
	
![[Pasted image 20241013114738.png]]
	这样一来，只需要操作同一个参数就行。

###### 在Shell函数中使用位置参数
	调用函数加上参数就可与脚本一样使用
##### 批量处理位置参数
	Shell提供了两个特殊参数 * & 两者均可扩展成完整的位置参数列表。
	
![[Pasted image 20241013115959.png]]

![[Pasted image 20241013120444.png]]
	![[Pasted image 20241013120558.png]]
	结论：使用 “$@" ，适用于大部分情况。

	