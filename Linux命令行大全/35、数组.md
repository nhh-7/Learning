	Bash中的数组只能是一维的。
###### 创建数组
	在访问数组变量时就会自动创建
	或使用declare 的 -a 选项创建
	declare -a a # 创建a数组
###### 为数组赋值
	使用[]访问数组元素与赋值
	或 赋多个值 name=(value1 value2 value3...)
	括号中也可以指定下标 name=([0]=value1 [1]=value2 [2]=value3)
##### 数组操作
###### 输出数组的全部内容
	[*]和[@]的方式可以访问数组的所有元素，但使用 @ 最佳。
	
![[Pasted image 20241013220641.png]]
###### 确定数组元素的数量
	通过参数扩展，使用类似获取字符串长度的方式确定数组中元素的数量。
	
![[Pasted image 20241013221051.png]]
	
	在Bash中，数组元素仅在被赋值的时候才存在，无论其索引是多少。
###### 确定存在数组元素的索引
	${!array[*]}
	${!array[@]}
	使用 @ 最佳。
	
![[Pasted image 20241013221615.png]]
###### 向数组尾部添加元素
	使用 += 操作符，可自动将元素添加到数组尾部。
	
![[Pasted image 20241013221820.png]]
###### 数组排序
![[Pasted image 20241013222330.png]]

###### 删除数组
	使用 unset 命令可以删除整个数组，也可以删除单个元素，对数组赋空值并不能删除数组。
	
![[Pasted image 20241013222616.png]]

##### 关联数组 
![[Pasted image 20241013222817.png]]

	必须使用declare -A进行 创建