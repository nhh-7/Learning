##### 分组命令与子shell
	分组命令使用花括号，子Shell使用括号。
	必须用空格符将花括号与命令隔开，且命令之间使用分号隔开，最后一个命令后必须以分号或换行符结尾。
	在重定向时，常用于将多个命令的结果合并成单一流。
###### 进程替换
	分组命令在当前Shell中执行命令，而子Shell则在当前Shell的副本中执行命令。因此大多数情况下，分组命令更为可取。

	管道中的命令总是在子Shell中执行 的，因此任何对变量赋值的命令在管道中都会发生问题。Shell提供 了进程替换的独特扩展方式 。
	进程替换通常结合 包含read的循环使用。
	
![[Pasted image 20241013230459.png]]
#####  陷阱
	trap argument signal 
	argument 是被作为命令读取的字符串，signal是信号名称，会触发执行相应的信号处理命令
	
![[Pasted image 20241013231121.png]]
	该脚本在运行时只要接收到了 SIGINT SIGTERM 信号就会执行echo命令。
	常见做法是指定Shell函数作为命令。
##### 使用wait实现异步执行
	Shell变量 $! 中包含最近置入后台的作业的PID

	在父脚本中执行子脚本，并使父脚本在子脚本结束之前先暂停执行。
	
![[Pasted image 20241013232402.png]]
![[Pasted image 20241013232233.png]]
##### 具名管道
	具名管道可用于在两个进程之间形成连接，也可以像其他类型文件那样使用。
	具名管道是一种先进先出的缓冲区。
###### 创建具名管道
	mkfifo命令
	
![[Pasted image 20241013232759.png]]
###### 使用具名管道