##### 扩展
	echo *  输出当前工作目录中的文件名 
	这是因为在执行echo命令之前，Shell会将*扩展为当前工作目录的文件名
###### 路径名扩展
![[Pasted image 20240919203129.png]]
	
	使用通配符就可以使得echo输出指定的路径
###### 波浪线扩展
	~ 用于单词之前时，会扩展为同名用户的主目录。如果未指定单词，则扩展为当前用户的主目录。
	echo ~username 会输出username用户的主目录
###### 算术扩展
	Shell通过扩展可以执行算术明算
	算术运算使用 $((expression)) 其中expression是算术表达式
	支持如下运算符
	
![[Pasted image 20240919203724.png]]
![[Pasted image 20240919203743.png]]
![[Pasted image 20240919203905.png]]
###### 花括号扩展
![[Pasted image 20240919204324.png]]
	
	花括号扩展可以按照指定模式创建多个文本字符串
	
![[Pasted image 20240919204458.png]]
	
	还可以输出具有前导0的数字
	
![[Pasted image 20240919204550.png]]
	
	还可以嵌套扩展
###### 命令替换
	命令替换允许我们使用命令的输出结果作为扩展结果，使用$符号将which cp的输出结果作为ls命令的参数。
	
![[Pasted image 20240919205416.png]]

##### 引用
	使用引用机制可以有选择地禁止不需要的扩展
###### 双引号
	使用双引号意味着单词分割、路径名扩展、波浪线扩展、花括号扩展全部失效，但参数扩展、算术扩展、命令替换仍然可用。
	
![[Pasted image 20240919210323.png]]	![[Pasted image 20240919210603.png]]
###### 单引号
	使用单引号可以禁止所有扩展
	
![[Pasted image 20240919210846.png]]
###### 转义字符
	若只想引用单个字符，可以在该字符前加上一个反斜线。使用转义功能消除某个字符的特殊性。
	
![[Pasted image 20240919211202.png]]
###### 反斜线转义序列
![[Pasted image 20240919211447.png]]

	echo的-e选项能够解释转义序列。也可以将转义序列放到$''中，如下：
	
![[Pasted image 20240919211725.png]]