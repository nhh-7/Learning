	ping：向网络主机发送ICMP ECHO_REQUEST分组
	traceroute：输出分组抵达网络主机所经历的路线
	ip：显示/操作路由、设备、策略路由、隧道。
	netstat：输出网络连接，路由表、接口统计、伪装连接、多播成员关系。
	ftp：互联网文件传输程序
	wget：非交互式网络下载工具
	ssh：远程登陆程序
	scp和sftp：在网络上复制文件
##### 网络检查与监控
###### ping
	核实网络连接的连通性
###### traceroute
	列出网络流量从本地系统到指定主机经过的所有跳数。
###### ip
![[Pasted image 20240925162809.png]]
	可看出，系统有两个网络接口，第一个接口是环回接口-lo，是一个虚拟接口，系统用它来“和自己对话”，第二个接口是ens33，是以太网接口。
	接口中第一行的UP表示接口已启用，第三行的inet表示ip地址。
###### netstat
	用于检查各种网络设置和统计信息。
	-ie选项能够检查系统的网络接口。
	
![[Pasted image 20240925163200.png]]

	-r选项能够显示内核的网络路由表
	
![[Pasted image 20240925163329.png]]
##### 通过网络从传输文件
###### ftp
###### lftp
###### wget
	命令行文件下载程序，可用于从WebheFTP网站下载文件，
##### 与远程主机的安全通信
###### ssh
	SSH由两部分组成：SSH服务器和SSH客户端。前者在远程主机中运行，负责在端口22上 监听接入的连接；后者在本地主机上运行，用于同远程SSH服务器通信。
###### scp和sftp
	这两个程序能够利用SSH加密隧道在网络上复制远程主机的文件。