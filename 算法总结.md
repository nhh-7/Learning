### 哈希表

- 1.两数之和： 使用哈希表记录，并O1判断是否存在与当前遍历所对应的数
- 49.字母异位词分组： 排序后的string作为键，排序前的string作为值
- 128.最长连续序列：将所有num放入map，遍历nums，根据map判断当前num+1的数是否存在，记录最长答案。

### 双指针

- 283.移动零：同向双指针，快指针遇到非0就将其移到满指针的位置
- 11.盛最多水的容器：相向双指针，每次遍历只移动left，right中对应高度较小的指针，并判断是否更新答案
- 15.三数之和：三根指针，i，j同向，k相向，先排序nums，i 遍历nums，循环内j为i+1，k为n-1，符合条件加入答案，否则移动j或者k。关键在于对指针的**去重逻辑**。
- 42.接雨水：相向双指针，记录当前左右的最大高度，以列为单位宽度为1，高度则为当前左右最大高度中的较小值减去当前高度，累加答案。

### 滑动窗口

滑动窗口一般都遵循三个步骤，移动右边界扩大，判断条件，移动左边界缩减

+ 3.无重复字符的最长子串：不定长窗口，用一个`[128]int`的数组来记录当前窗口中字符的出现次数，遍历过程中先扩大右边界，如果有次数大于1，那么反复右移左边界，每次记录答案
+ 438.找到字符串中所有字母异位词：定长窗口，与p的长度相同，使用`[26]int`数组记录窗口中的字符出现次数，当窗口中字符与p中次数相同时，记录答案
+ 76.最小覆盖子串：不定长窗口，每次遍历中进行 加入窗口 -> 判断条件 -> 缩减窗口 

### 子串

##### 前缀和

* 560.和为k的子数组：计算出前缀和数组，遍历这个前缀和数组，并使用map记录前缀和出现的次数，判断map中是否存在 当前遍历到的前缀和`sj`- k 累加答案。
  进一步优化，可以直接边遍历nums边记录前缀和，只需一次循环。
* 2602.使数组元素全部相等的最少操作次数：对于每次query，就是计算每个num与query的绝对值数值，nums中有大的有小的，通过排序，二分查找快速找到分界线，在通过前缀和结果分别计算左右两部分的差值
* 238.除了自身以外数组的乘积：利用两个额外数组分别存储每个位置左侧所有元素的乘积和右侧所有元素的乘积，最后将左右乘积相乘得到最终结果

##### 单调队列

* 239.滑动窗口最大值：维护一个单调队列（递减），保证队头就是当前窗口的最大值，当窗口大小到达k，则可以记录答案

### 数组

##### 贪心

* 53.最大子数组和：从头到尾遍历，如果当前累计和变负了，就果断清零，从当前元素重新开始算，同时随时更新遇到过的最大和。 
* 56.合并区间： 先进行排序（保证start升序，若start相同则end升序），left、right记录区间，并不断移动。

189.轮转数组： 三次原地翻转操作即可

41.缺失的第一个正数：利用数组本身作为哈希表，将数字`i`放到下标索引`i-1`的位置，最后遍历数组找到第一个`nu ms[i]`不等于`i+1`的位置，`i+1`即为答案。

### 矩阵

* 73.矩阵置零：先扫一遍记录哪些行 / 列要置零，再扫一遍把这些行 / 列全部置零（用第一行第一列当标记），需要记录原来第一行第一列是否有零。
* 54.螺旋矩阵：定义顺时针方向数组（右、下、左、上），当碰到边界或以访问元素则改变到下一个方向。
* 48.旋转图像：先将矩阵**沿主对角线（左上到右下）转置（交换）**，再将每一行**水平翻转**。
* 240.搜索二维矩阵 II：从矩阵**右上角**开始查找，利用矩阵「行递增、列递减」的特性，当前元素大于目标则向左移一列，小于目标则向下移一行，等于则找到，直至越界则不存在。

### 链表

* 160.相交链表：让两个指针分别从两条链表头出发，遍历到末尾后切换到另一条链表头继续遍历，若链表相交则两指针必会在相交节点相遇，若不相交则同时走到链表末尾（均为 nil）。（还有一种提前对齐链表长度的解法）
* 206.反转链表：用**迭代法**从头遍历链表，逐个改变节点的 `Next` 指针指向（指向前一个节点），同时用临时变量保存后驱节点避免断链
* [92. 反转链表 II](https://leetcode.cn/problems/reverse-linked-list-ii/)：创建虚拟头节点指向链表头，先将指针移动到「反转起始位置的前驱节点」，再从该位置开始「反转 left 到 right 之间的节点（共 right-left+1 个）」（参考T206），最后将反转后的子链表首尾分别拼接回原链表，完成指定区间的反转。
* [25. K 个一组翻转链表](https://leetcode.cn/problems/reverse-nodes-in-k-group/)：将链表按 K 个一组拆分，每组内部执行局部翻转，并由一个“哨兵位”的前驱节点（prev）负责将翻转后的各段重新首尾相连。参考T206、T92
* 234.回文链表：先用快慢指针找到链表**中间节点**，反转后半段链表，再用两个指针分别从链表头和反转后的后半段头开始逐一比对节点值，全部相等则为回文
* 141.环形链表：用**快慢指针**（慢指针每次走 1 步，快指针每次走 2 步）遍历链表，若两指针相遇则链表存在环，若快指针先走到`nil`则无环。
* 142.环形链表 II： 先用快慢指针（慢 1 步、快 2 步）找到相遇点，找到之后，用两个指针分别从head、相遇点开始，同步移动（1步），相遇则为环入口。
* 21.合并两个有序链表：创建一个虚拟头节点作为合并后链表的起点，用两个指针分别遍历两个有序链表，每次选择值更小的节点接入新链表，直至其中一个链表遍历完，最后将未遍历完的链表剩余部分直接接入新链表末尾。
* [23. 合并 K 个升序链表](https://leetcode.cn/problems/merge-k-sorted-lists/)：利用**最小堆**维护 K 个链表的当前头节点，每次弹出最小值并补入其下一个节点
* 2.两数相加：模拟十进制加法的「竖式计算」过程：用双指针遍历两个链表（从低位到高位），逐位相加并记录进位，将每一位的计算结果（和 %10）作为新节点接入结果链表，最后若仍有进位则补充一个值为进位的节点。
* 445.两数相加 II
  1. 反转链表后相加
  2. 先压入栈，在从栈顶弹出进行低位相加
* 19.删除链表的倒数第 N 个结点：用**快慢指针**（快指针先领先慢指针 N 步），再让快慢指针同步遍历至链表末尾，此时慢指针恰好指向倒数第 N 个结点的前驱结点，直接跳过目标结点即可完成删除；配合虚拟头节点可避免处理「删除头结点」的边界场景。
* [24. 两两交换链表中的节点](https://leetcode.cn/problems/swap-nodes-in-pairs/)：以虚拟头节点为初始前驱（node0），链表头为第一个待交换节点（node1），每次先保存 node2（node1 的后继）和 node3（node2 的后继），再依次修改指针让 node0 指向 node2、node2 指向 node1、node1 指向 node3，最后将 node0 更新为 node1、node1 更新为 node3，循环至剩余节点不足两个，完成两两交换。
* [138. 随机链表的复制](https://leetcode.cn/problems/copy-list-with-random-pointer/)：在每个原节点后面复制一个新节点（`A -> A' -> B -> B'`），利用这种交错关系设置 `random` 指针，最后将新旧链表拆分开来。
* [148. 排序链表](https://leetcode.cn/problems/sort-list/)：归并排序，利用快慢指针寻找链表中点进行递归拆分，再通过“合并两个有序链表”的逻辑将子链表缝合
* [146. LRU 缓存](https://leetcode.cn/problems/lru-cache/)：利用**哈希表**（key to node）实现快速查找，配合**双向链表**（维护链表头尾虚拟节点）实现O(1)级别的节点移动（将最近访问的提到表头，超额时删除表尾）。

### 二叉树

* 中序遍历（其余顺序同理）：
  * 递归：利用函数调用栈，按“递归左子树、记录当前根值、递归右子树”的顺序深度优先搜索。
  * 迭代：使用栈模拟递归，通过为节点标记颜色（如白色表示初次遇到，那么将其子树加入栈-按照遍历的反顺序，灰色表示已处理过他的子节点，那么直接访问该节点）来精确控制节点入栈和出栈打印的时机。模拟递归：当第一次遍历节点时，需要向下探索子树，探索结束后才会回来处理该节点。
* [104. 二叉树的最大深度](https://leetcode.cn/problems/maximum-depth-of-binary-tree/)：二叉树的最大深度等于**左、右子树最大深度的较大值加 1**（即 max(leftDepth, rightDepth) + 1），递归到底部空节点时深度为 0。（当前节点的高度由其子树决定 - 由下往上传播，所以一定是后序遍历）
*  [226. 翻转二叉树](https://leetcode.cn/problems/invert-binary-tree/)：前序或后序递归，递归地交换每个节点的左、右子节点
* [101. 对称二叉树](https://leetcode.cn/problems/symmetric-tree/)：将判断对称转化为判断**两棵树是否互为镜像**：即左树的左子节点与右树的右子节点比较，左树的右子节点与右树的左子节点比较，值均相等且结构相同。
* [543. 二叉树的直径](https://leetcode.cn/problems/diameter-of-binary-tree/)：二叉树的直径等于**左子树深度 + 右子树深度**的最大值，在递归计算每个节点深度的过程中，顺便更新全局最大直径。
* [124. 二叉树中的最大路径和](https://leetcode.cn/problems/binary-tree-maximum-path-sum/)：对于每个节点，计算它能为父节点提供的**单侧最大贡献值**（root + max(left, right, 0)），并在递归过程中顺便更新**全局最大路径和**（left + right + root）。和T543的区别在于本题节点的值可能是负数，所以要和0比较取大值。
* [102. 二叉树的层序遍历](https://leetcode.cn/problems/binary-tree-level-order-traversal/)：利用**队列**先进先出的特性，通过记录当前队列的长度（Size）来控制每一轮循环仅处理同一层的节点，从而实现分层输出。
* [108. 将有序数组转换为二叉搜索树](https://leetcode.cn/problems/convert-sorted-array-to-binary-search-tree/)：取有序数组的中点作为根节点，递归地将中点左侧部分转换为左子树，中点右侧部分转换为右子树，从而确保树的平衡性。（自顶向下）
* [98. 验证二叉搜索树](https://leetcode.cn/problems/validate-binary-search-tree/)：利用二叉搜索树（BST）**中序遍历是严格递增序列**的特性，在遍历时检查当前值是否大于前驱值（定义一个前驱节点指针，在遍历中不断更新）
* [230. 二叉搜索树中第 K 小的元素](https://leetcode.cn/problems/kth-smallest-element-in-a-bst/)：利用二叉搜索树（BST）**中序遍历结果为升序**的特性，在遍历时维护一个计数器，当访问到第 k个节点时直接返回结果并提前终止。
* [199. 二叉树的右视图](https://leetcode.cn/problems/binary-tree-right-side-view/)：利用**层序遍历**记录每一层的最后一个节点
* [114. 二叉树展开为链表](https://leetcode.cn/problems/flatten-binary-tree-to-linked-list/)：对于每个节点，将其左子树整体插入到父节点与右子树之间，具体做法是将原右子树上连接到左子树的最右下节点（前驱节点，关键就在**找这个前驱节点**），然后将整个左子树移至右侧。
* [105. 从前序与中序遍历序列构造二叉树](https://leetcode.cn/problems/construct-binary-tree-from-preorder-and-inorder-traversal/)：前序序列的第一个元素是**根节点**，通过它在中序序列中找到位置，从而将数组切分为左、右两棵子树的序列，递归构建即可。
* [437. 路径总和 III](https://leetcode.cn/problems/path-sum-iii/)：在递归遍历树的同时，利用**哈希表**记录从根节点到当前节点路径上的“前缀和”出现次数，通过查找 `当前前缀和 - targetSum` 的差值来快速计算以当前节点为终点的合法路径数。
* [236. 二叉树的最近公共祖先](https://leetcode.cn/problems/lowest-common-ancestor-of-a-binary-tree/)：从根节点开始递归寻找 p 或 q，通过**后序遍历**自底向上返回发现情况：若左右子树各发现一个目标，则当前节点即为最近公共祖先

### 图论

* [200. 岛屿数量](https://leetcode.cn/problems/number-of-islands/)：遍历整个网格，每当遇到陆地 `'1'` 时，岛屿总数加 1，并以此为起点通过 **DFS 或 BFS** 将所有相连的陆地全部“淹没”（修改为 `'0'`），直到遍历结束。
* [994. 腐烂的橘子](https://leetcode.cn/problems/rotting-oranges/)：将所有初始腐烂的橘子同时作为起点存入**队列**进行 BFS，逐层（按分钟）向四周扩散感染新鲜橘子，直到无法继续扩散为止。记录初始新鲜橘子的个数，最后用于判断是否全部被感染。
* [207. 课程表](https://leetcode.cn/problems/course-schedule/)：将课程依赖看作**有向图**，统计每门课的**入度**，不断移除入度为 0 的课并更新其后续课程的入度，若最终修读总数等于课程总数，则说明无环。
* [208. 实现 Trie (前缀树)](https://leetcode.cn/problems/implement-trie-prefix-tree/)：前缀树是一种多叉树，每个节点存储一个字符到子节点的映射（通常是长度为 26 的数组），并有一个布尔值 `isEnd` 标记该节点是否为一个完整单词的结尾。

### 回溯

* [46. 全排列](https://leetcode.cn/problems/permutations/)：通过**递归**尝试在每个位置填入**尚未使用**的数字，并在尝试完毕后进行**回溯**（撤销选择），从而穷举出所有可能的排列组合。
* [78. 子集](https://leetcode.cn/problems/subsets/)：递归用于加入元素或回溯踢出元素，递归中的循环使用start来避免出现顺序不同但重复的组合。（在递归中从 start 开始遍历，避免产生 [1,2] 和 [2,1] 这种重复组合）
* [17. 电话号码的字母组合](https://leetcode.cn/problems/letter-combinations-of-a-phone-number/)：在递归中保存当前访问的数字下标，通过递归逐位处理输入的数字，每一层递归代表数字串中的一个位置，尝试该位置对应的所有可能字母。
* [39. 组合总和](https://leetcode.cn/problems/combination-sum/)：通过递归搜索所有数字组合，每一步决定是否继续使用当前数字或跳到下一个数字，在递归函数中的循环遍历中让下一层递归的 `start`为当前遍历的`i`而不是`i+1`，利用 `start` 索引来确保搜索始终向后进行，从而避免产生如 `[2, 3]` 和 `[3, 2]` 这种重复的集合。
* [22. 括号生成](https://leetcode.cn/problems/generate-parentheses/)：在递归中**左括号数量小于 n** 才能加左括号，**右括号数量小于左括号数量** 才能加右括号。
* [79. 单词搜索](https://leetcode.cn/problems/word-search/)： **DFS** 尝试向四个方向匹配单词字符，匹配时通过**原地修改**（标记为已访问）防止字符重复使用，若路径不通则在回溯时**还原字符**。
* [131. 分割回文串](https://leetcode.cn/problems/palindrome-partitioning/)：从字符串起始位置开始，枚举所有可能的分割点，如果当前分割出的前缀是**回文串**（使用start用于记录切割点），则递归处理剩余子串；当处理到字符串末尾时，记录下这一种分割方案。
* [51. N 皇后](https://leetcode.cn/problems/n-queens/)：每一层递归函数处理一行，在递归中的循环用于判断该行中的每一列

### 二分查找

使用闭区间[left, right]进行二分查找。

* [35. 搜索插入位置](https://leetcode.cn/problems/search-insert-position/)：在有序数组中，通过不断比较中间元素与目标值来折半搜索范围；如果目标值不存在，最终查找停止的左边界索引 `left` 即为它应该被插入的位置。
* [74. 搜索二维矩阵](https://leetcode.cn/problems/search-a-2d-matrix/)：由于矩阵每一行都是有序的，且每一行的第一个整数大于前一行的最后一个整数，我们可以将这个二维矩阵逻辑上视为一个**一维有序数组**，然后直接进行二分查找
*  [34. 在排序数组中查找元素的第一个和最后一个位置](https://leetcode.cn/problems/find-first-and-last-position-of-element-in-sorted-array/)：通过两次二分查找，一次专门寻找目标值的**左边界**（第一个出现的位置），一次专门寻找目标值的**右边界**（最后一个出现的位置）。
  **寻找左边界：** 即使找到了 `target`，也要继续向**左**收缩区间（`right = mid - 1`），直到找到最左边的那个。
  **寻找右边界：** 即使找到了 `target`，也要继续向**右**收缩区间（`left = mid + 1`），直到找到最右边的那个。

### 栈

### 堆

### 贪心

### 动态规划

##### 背包问题

 0/1 背包模板 (物品有限)

```go
// weights: 物品重量, values: 物品价值, W: 背包总容量
func knapsack01(weights []int, values []int, W int) int {
    dp := make([]int, W+1) // 默认初始化为0
    
    // 遍历物品
    for i, w := range weights {
        v := values[i]
        // 遍历容量：【关键】必须倒序！保证物品只被放入一次
        for j := W; j >= w; j-- {
            dp[j] = max(dp[j], dp[j-w] + v)
        }
    }
    return dp[W]
}
```

完全背包模板（物品无限）

```go
func knapsackUnbounded(weights []int, values []int, W int) int {
    dp := make([]int, W+1)
    
    // 遍历物品
    for i, w := range weights {
        v := values[i]
        // 遍历容量：【关键】必须正序！因为当前物品可以被多次使用
        for j := w; j <= W; j++ {
            dp[j] = max(dp[j], dp[j-w] + v)
        }
    }
    return dp[W]
}
```

如果题目是求**“排列数”**还是**“组合数”**，循环顺序会有变化（常见于完全背包）：

1. **求组合数**（顺序无关，如 `[1, 2]` 和 `[2, 1]` 视为同一种）：
   - **先遍历物品，再遍历背包**。
   - 典型题目：LeetCode 518 零钱兑换 II。
2. **求排列数**（顺序有关，如 `[1, 2]` 和 `[2, 1]` 是不同的）：
   - **先遍历背包，再遍历物品**。
   - 典型题目：LeetCode 377 组合总和 Ⅳ。

### 多维动态规划

